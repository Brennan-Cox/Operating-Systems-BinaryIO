This package contains a reader and writer programmed in c++ which
accepts as arguments: (the file name of a binary file to store numbers)

After running the reader.exe the reader will read all data as an int data
type until it reaches EOF

After running the writer.exe the writer will continue to take and write
integer inputs in binary format on the file until the user inputs a -1